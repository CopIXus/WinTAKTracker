# Fictional enrollment examples for WinTAKTracker docs/tests.
# DO NOT replace these with real Portal URLs, hosts, tokens, or callsigns.

# OpenTAK Tracker–style
opentaktracker://enroll?host=tak.example.com&username=USER&token=TOKEN&callsign=CALLSIGN&team=Cyan&role=Team%20Member

# ATAK / TAK Portal enroll-style (username+token → Marti CSR on 8446, then CoT SSL 8089)
tak://com.atakmap.app/enroll?host=tak.example.com&username=USER&token=TOKEN

# Optional connect-string host (stream port + protocol; enrollment still uses 8446)
tak://com.atakmap.app/enroll?host=tak.example.com:8089:ssl&username=USER&token=TOKEN

# ATAK preference-style (configure-only). WinTAKTracker appends .wtt to callsign if missing.
tak://com.atakmap.app/preference?locationCallsign=CALLSIGN&locationTeam=Cyan&role=Team%20Member

# iTAK CSV-style
ExampleServer,tak.example.com,8089,ssl
